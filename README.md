This repository demonstrates a relatively clean commit graph created by two developers using rebase on their local clones rather than trivial merges.

Compare with [merge-bubbles](../../../merge-bubbles).
